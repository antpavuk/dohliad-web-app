import { SxProps, Theme } from '@mui/material';

export const formControlStyle: SxProps<Theme> = {
  width: '100%',
  margin: '20px auto',
  display: 'flex',
  flexDirection: 'column',
  gap: '10px'
};

export const submitButtonStyle: SxProps<Theme> = { margin: '10px auto', width: '100%' };
